import { createTheme } from '@mui/material';

export const theme = createTheme({
  typography: {
    allVariants: {
      fontFamily: '"Lora", "Arial", sans-serif',
    },
    body1: {
      fontSize: 19,
    },
    h5: {
      fontSize: 30,
    },
  },
});
